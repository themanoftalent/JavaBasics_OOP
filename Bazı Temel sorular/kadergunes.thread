public class MultiThread {

    public static void main(String[] args) {

        // Yeni bir thread (iş parçağı) oluştur
        Thread is_parcacigi = new Thread(() -> {
            System.out.println("Hello Thread");
        });

        // İş parçacığını başlat
        is_parcacigi.start();
    }
}
